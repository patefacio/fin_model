//! Display implementations

////////////////////////////////////////////////////////////////////////////////////
// --- module uses ---
////////////////////////////////////////////////////////////////////////////////////
use crate::EmployerMatchProgram;
use crate::RetirementContribution;
use crate::RetirementContributionLimit;
use core::fmt::Display;
use core::fmt::Formatter;

////////////////////////////////////////////////////////////////////////////////////
// --- trait impls ---
////////////////////////////////////////////////////////////////////////////////////
impl Display for RetirementContribution {
    /// Format the instance.
    ///
    ///   * **f** - Formatter to push formatted item to.
    ///   * _return_ - Formatted instance
    fn fmt(&self, f: &mut Formatter<'_>) -> core::fmt::Result {
        // α <fn Display::fmt for RetirementContribution>
        todo!("Implement `fmt`")
        // ω <fn Display::fmt for RetirementContribution>
    }
}

impl Display for EmployerMatchProgram {
    /// Format the instance.
    ///
    ///   * **f** - Formatter to push formatted item to.
    ///   * _return_ - Formatted instance
    fn fmt(&self, f: &mut Formatter<'_>) -> core::fmt::Result {
        // α <fn Display::fmt for EmployerMatchProgram>
        todo!("Implement `fmt`")
        // ω <fn Display::fmt for EmployerMatchProgram>
    }
}

impl Display for RetirementContributionLimit {
    /// Format the instance.
    ///
    ///   * **f** - Formatter to push formatted item to.
    ///   * _return_ - Formatted instance
    fn fmt(&self, f: &mut Formatter<'_>) -> core::fmt::Result {
        // α <fn Display::fmt for RetirementContributionLimit>
        todo!("Implement `fmt`")
        // ω <fn Display::fmt for RetirementContributionLimit>
    }
}

// α <mod-def retirement_display>
// ω <mod-def retirement_display>
