//! Display implementations

////////////////////////////////////////////////////////////////////////////////////
// --- module uses ---
////////////////////////////////////////////////////////////////////////////////////
use crate::GrowthAssumption;
use crate::GrowthItemMappings;
use crate::ItemGrowth;
use crate::MarketAssumptions;
use crate::OutlookMarketAssumptions;
use crate::SystemCorrelationEntry;
use crate::SystemCorrelationMatrix;
use crate::SystemGrowthId;
use core::fmt::Display;
use core::fmt::Formatter;

////////////////////////////////////////////////////////////////////////////////////
// --- trait impls ---
////////////////////////////////////////////////////////////////////////////////////
impl Display for GrowthAssumption {
    /// Format the instance.
    ///
    ///   * **f** - Formatter to push formatted item to.
    ///   * _return_ - Formatted instance
    fn fmt(&self, f: &mut Formatter<'_>) -> core::fmt::Result {
        // α <fn Display::fmt for GrowthAssumption>
        todo!("Implement `fmt`")
        // ω <fn Display::fmt for GrowthAssumption>
    }
}

impl Display for ItemGrowth {
    /// Format the instance.
    ///
    ///   * **f** - Formatter to push formatted item to.
    ///   * _return_ - Formatted instance
    fn fmt(&self, f: &mut Formatter<'_>) -> core::fmt::Result {
        // α <fn Display::fmt for ItemGrowth>
        todo!("Implement `fmt`")
        // ω <fn Display::fmt for ItemGrowth>
    }
}

impl Display for SystemGrowthId {
    /// Format the instance.
    ///
    ///   * **f** - Formatter to push formatted item to.
    ///   * _return_ - Formatted instance
    fn fmt(&self, f: &mut Formatter<'_>) -> core::fmt::Result {
        // α <fn Display::fmt for SystemGrowthId>
        todo!("Implement `fmt`")
        // ω <fn Display::fmt for SystemGrowthId>
    }
}

impl Display for GrowthItemMappings {
    /// Format the instance.
    ///
    ///   * **f** - Formatter to push formatted item to.
    ///   * _return_ - Formatted instance
    fn fmt(&self, f: &mut Formatter<'_>) -> core::fmt::Result {
        // α <fn Display::fmt for GrowthItemMappings>
        todo!("Implement `fmt`")
        // ω <fn Display::fmt for GrowthItemMappings>
    }
}

impl Display for SystemCorrelationEntry {
    /// Format the instance.
    ///
    ///   * **f** - Formatter to push formatted item to.
    ///   * _return_ - Formatted instance
    fn fmt(&self, f: &mut Formatter<'_>) -> core::fmt::Result {
        // α <fn Display::fmt for SystemCorrelationEntry>
        todo!("Implement `fmt`")
        // ω <fn Display::fmt for SystemCorrelationEntry>
    }
}

impl Display for SystemCorrelationMatrix {
    /// Format the instance.
    ///
    ///   * **f** - Formatter to push formatted item to.
    ///   * _return_ - Formatted instance
    fn fmt(&self, f: &mut Formatter<'_>) -> core::fmt::Result {
        // α <fn Display::fmt for SystemCorrelationMatrix>
        todo!("Implement `fmt`")
        // ω <fn Display::fmt for SystemCorrelationMatrix>
    }
}

impl Display for MarketAssumptions {
    /// Format the instance.
    ///
    ///   * **f** - Formatter to push formatted item to.
    ///   * _return_ - Formatted instance
    fn fmt(&self, f: &mut Formatter<'_>) -> core::fmt::Result {
        // α <fn Display::fmt for MarketAssumptions>
        todo!("Implement `fmt`")
        // ω <fn Display::fmt for MarketAssumptions>
    }
}

impl Display for OutlookMarketAssumptions {
    /// Format the instance.
    ///
    ///   * **f** - Formatter to push formatted item to.
    ///   * _return_ - Formatted instance
    fn fmt(&self, f: &mut Formatter<'_>) -> core::fmt::Result {
        // α <fn Display::fmt for OutlookMarketAssumptions>
        todo!("Implement `fmt`")
        // ω <fn Display::fmt for OutlookMarketAssumptions>
    }
}

// α <mod-def growth_display>
// ω <mod-def growth_display>
